# ♀ Planet Venus for Plone — Apache HTTPD Configuration
# =====================================================
#
# This file is normally included from the Apache HTTPD setup.

DocumentRoot ${buildout:htdocs-directory}
ServerAdmin ${buildout:admin-email}
<Directory ${buildout:htdocs-directory}>
    Order Allow,Deny
    Allow from all
</Directory>
